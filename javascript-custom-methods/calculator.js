/* exported calculator */

const calculator = {
  add: function (x, y) {
    return x + y;
  },
  subtract: function (x, y) {
    return x - y;
  },
  // multiply property being //
  multiply: function (x, y) {
    return x * y;
  },
  divide: function (x, y) {
    return x / y;
  },
  sqaure: function (x) {
    const squared = x * x;
    return squared;
  }

};
