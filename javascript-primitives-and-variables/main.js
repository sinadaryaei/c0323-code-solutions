
// the string sina daryaei is assgned to variable fullNmae //

const fullName = 'Sina Daryaei';

// the log method of the console object is being called with two arguemments: the first being a string and the second one being a variable //

console.log('value of fullName:', fullName);

// the boolean true is assigned to the variable isCool //

const isCool = true;

console.log('value of isCool:', isCool);

// the number 2 is assigned to the variabl totalPets //

const totalPets = 2;

console.log('value of totalPets:', totalPets);

console.log('tyeof fullName:', typeof fullName);

console.log('typeof of isCool:', typeof isCool);

console.log('typeof of totalPets:', typeof totalPets);

// null is being assigned to the variable uknown. //

const unknown = null;

console.log('value of unknown:', unknown);

console.log('typeof of unknown:', typeof unknown);

// the variable never has no assigned value. //

let never;

console.log('value of never:', never);

console.log('type of never:', typeof never);
